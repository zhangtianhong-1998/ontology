# 字段发现与无 Docker 互操作验证（2026-09-23）

本代码与设计文档分支不包含历史 `runs/` 目录；下方旧版运行文件链接只在保留该目录的本地研究工作区可打开。当前版本可按[离线说明](DATAHUB_OFFLINE.md)重新生成合成运行结果。

- 当前代码全套合成测试：`uv run --locked pytest -q`，**75 项通过**。测试覆盖 P01 分组统计、P03 有界召回、P04 精确验证、候选预算与增量上下文注入；不测真实业务语义正确率。
- 本机 macOS Python 3.14.3 的独立虚拟环境同样运行 **75 项测试通过**（66.06 秒），模拟构建和 DataHub 文件导出也完成；它不替代 Windows 11 / Python 3.14.7 真机测试。
- 独立的代码与文档分支检出副本（含 11 个合成夹具文件）运行 **74 项通过、1 项跳过**；跳过项依赖未随代码分支提供的 KPIOnto 文件。该副本的 mock 端到端构建为 `complete`，不需要真实数据或上游源码子模块。
- `uv run --locked ontology-r2 build --config config/runtime.mock.yaml --output .cache/smoke-no-docker-20260923`：2 表、13 条合成记录、4 个候选及 4 个核验，模拟 LLM 8 次调用，`manifest.status=complete`。
- `ontology-r2 export-datahub` 对该运行导出 2 个 dataset、4 个 aspect；DataHub 1.7.0.12 `datahub check metadata-file` 报 `4 MCEs found - all valid`，可选 Lite 脚本写入并读回 4 个 aspect。此结果不说明 Lite 具备图遍历或血缘。
- `datahub check` 是本机 CLI 校验，没有配置远端入库；其上游实现默认开启使用情况遥测。初次校验未显式关闭遥测，因此只能确认未执行元数据上传，不能保证当时没有发出使用事件。后续本地校验显式关闭遥测，Lite 导入脚本也在 SDK 导入前关闭它。
- 为 Windows x64 / CPython 3.14 下载了路线2与 DataHub Lite 的二进制轮子，并分别用 `pip download --no-index` 验证依赖闭包；同一环境合装存在 `uvicorn` 版本冲突，因此运行时需要两个虚拟环境。**尚未在 Windows 真机安装或执行**。
- 单列候选采样与源/目标各百万行核验在内存合成 DuckDB 中通过；该计时不能推断实际 CSV 磁盘、宽表和企业数据性能。

当前实现范围见 [状态表](STATUS.md)，Windows 安装步骤见 [离线说明](DATAHUB_OFFLINE.md)。以下保留早期版本的验证记录，不作为上述新模块的测试结果。

---

# LLM 双接口与英文路径迁移验证

本文中的代码、配置路径及命令均以 `prototypes/route2/` 为基准。

验证日期：2026-09-23；版本 0.3.0。

- 本地完整工程：`uv run --locked --no-sync pytest -q`，61 项通过、0 项失败，18.32 秒。
- 独立代码包：60 项通过、1 项跳过、0 项失败，24.35 秒；跳过的是未随包提供文件的 KPIOnto 集成测试。
- 新增接口用例 22 项：JSON/SSE、中文参数跨分片、四种思考关闭参数、原生 ReAct 共用配置、环境变量与缓存、断流、截断、坏 JSON、超时及服务拒绝参数。
- 使用本地 HTTP 模拟模型验证实际请求，未调用外部模型。流式用量只累计一次，隐藏思考内容与密钥不写入轨迹。
- 英文命名覆盖三个路线的自研目录、文档和配置；9 个上游子模块的版本未变。文档正文仍为中文，历史研究原件及上游源码内部命名保留。
- 代码及模板哈希：`c7f364d1e4675efc348ad05f1b9f46e964f20227e7a69ac203b1c72fb8e9cf56`。
- 迁移后完整模拟流程：`runs/transport-config-20260923-001`，状态 complete，2 表、13 条输入，10 次模拟调用，仍生成本地结果页面。

本轮没有实现字段发现 P01—P06，完成状态见 [STATUS.md](STATUS.md)。在线模型是否支持具体思考参数、真实语义质量及企业 MCP 仍需分别验证。

---

# 0.2.0 及更早验证记录（保留）

# 路线2验证报告：增量构建、ReAct 和可视化

验证日期：2026-09-23。版本 0.2.0。完成 RIGOR 源码对照后，已实现全字段直接映射、逐表增量与回滚、AgentScope 原生 ReAct 和本地结果页面。真实业务语义仍未用在线模型验证。

## 本轮验证

- `uv sync --locked --extra test --offline` 成功；`pytest -q`：**39 项通过，0 项失败，8.74 秒**。
- 代码及页面模板哈希：`1aaf52a54dbc59d76e735d7cc13ef44221390526178529adffacc5652c1bf0f7`。模型、MCP 和原版 RIGOR 的上游源码均未修改。
- 增量测试覆盖完整字段映射、拒绝后保留原 core、后续单元读取已接受类型、Judge 修正再次校验、跨单元覆盖拒绝、声明 FK 顺序与循环依赖。
- 原生 ReAct 测试覆盖自主二次查询、读原文、冲突摘要、伪造摘录、未读文档、单元外证据、调用预算和未注册工具。固定脚本仅用于驱动测试，不是另一套替代 Agent 循环。
- SDK 通过本地兼容 HTTP 服务实测：普通增量流程 4 次请求 / 80 个测试 token；原生 ReAct 3 次请求 / 60 个测试 token。stdio MCP 另有真实协议集成测试。没有调用外部 LLM。
- 可视化测试覆盖预览数量限制和 HTML 注入文本；已在本地浏览器检查实例关系详情、来源原文、范围、构建步骤和知识状态。
- 离线 wheel 构建成功，安装包包含可视化模板 `ontology_r2/viewer.html`。

## 新旧流程对照

旧版在改造前重新运行并保留 [基准](../../route2/runs/before-react-20260923-100758/baseline.yaml)；新版对应 [运行记录](../../route2/runs/react-incremental-20260923-002/manifest.yaml) 和 [结果页面](../../route2/runs/react-incremental-20260923-002/viewer.html)。两次输入快照一致。

| 项目 | 旧版整体规划 | 新版逐表增量 |
|---|---:|---:|
| 输入表 / 记录 | 2 / 13 | 2 / 13 |
| 输出对象 | 13 | 13 |
| 对象关系 | 3 | 3 |
| 未决项 | 3 | 3 |
| `has` 数据断言 | 13 | 66 |
| E2F 模拟模型调用 | 8 | 10 |
| 显式直接映射字段 | 未单独保证 | 8 / 8 |
| 可追溯增量步骤 | 无 | 2 个接受步骤 |

三个关系的源/目标行仍为 `(1→1)、(2→2)、(8→3)`；缺目标、多义目标、条件未知各 1 项。增加的 `has` 主要来自原字段复制，**不代表新发现了更多业务关系**。未强行对齐 KPIOnto，中性样本的外部映射仍为 `unmapped`。

## 规模与调用边界

- 23 表无外键的端到端测试通过：71 个字段全部进入直接映射，23 个工作单元，46 次模拟模型调用；该项关闭 MCP 和外部模型。调用次数随构建单元增加，不随记录逐行增长。
- [一万条源记录回归](../../route2/runs/react-scale-10000-20260923/benchmark.yaml)：总计 10,005 条输入，检查全部 10,000 条源记录，生成 3,750 条对象关系，未决 3,750 条；另有不适用和空引用各 1,250 条。**8 次模拟模型调用**，无未处理源记录。
- 一万条回归耗时 21.197 秒，主进程峰值约 247.94 MiB。新版保存更多原字段断言并生成页面，输出工作量与旧版不同；这些单次时长不是算法吞吐率对照。
- 本轮没有重跑百万完整语义抽取。下方旧版百万受限实验只属于旧代码，不冒充新版性能结果。

## 未覆盖的实际环境

真实模型效果、企业 MCP 的实际参数与文档质量、真实数据的关联召回及多次在线运行稳定性仍需配置后验证。字段统计升级 P01—P06、复杂公式、跨快照身份及证据失效仍按各自设计推进。结构校验、固定案例及 Agent 工具调用成功均不能替代业务语义验收。

---

# 改造前验证记录（保留）

验证日期：2026-09-23。以下为改造前版本的记录；模拟数据、模拟 MCP 和 AgentScope SDK 接口测试通过。真实模型效果尚未验证。机器可读记录见 [validation_results.yaml](../../route2/validation_results.yaml)。

## 1. 代码与测试

- 执行 `uv run pytest -q`：**27 项通过，0 项失败**，本次输出耗时 4.92 秒。
- 被验证代码哈希：`666eadeeb03e1661bc80c15102b67e31bcebdd123b2f3a3328afb604e7aea851`。各运行的 `manifest.yaml` 同时保存代码、输入、配置、提示词和本体哈希。
- 覆盖无外键的 23 表输入、复合键、跨 schema 同名表、重复 YAML 键、无关系输入、条件引用、JSON 局部引用、公式位置及未知函数、显式成员与观察样本、不同条件保留、预算上限、缓存回放、外部术语对齐和 MCP 异常状态。
- AgentScope 2.0.8 已通过本地 HTTP 兼容服务完成 2 次实际 SDK 请求，测试服务返回的 token 统计为 40；这验证接口调用与统计，不证明真实模型的语义能力。
- 输入角色和模型响应由合成夹具明确给定。测试结果证明这些已知模式的程序行为，不能用于宣称通用本体抽取正确率。

## 2. 完整模拟流程

[本次运行](../../route2/runs/mock-full-001/manifest.yaml) 使用 E2F：模拟数据、真实 stdio MCP 协议、模拟模型响应、本地 KPIOnto。

- 共导入 13 条记录，其中 8 条源记录、5 条目标定义。
- 输出 3 条预设记录关系，目标分别为 `(1→1)、(2→2)、(8→3)`；分别保留 1 条缺目标、1 条目标多义和 1 条条件未知。
- 另有 1 条明确不适用、1 条空引用；没有将全部记录强行关联。
- 执行三轮检索，只有带原文引用的有效 claim 进入生成上下文；原始工具返回保留在本地轨迹。
- KPIOnto 导入 29 张参考卡。中性合成数据没有强行映射到经营本体，结果为 `unmapped`；带翻译召回和引用校验的正向映射另用可控合成参考模型测试。
- 共 8 次模拟模型调用，结构引用检查通过。16 条断言中包含 13 条 `has` 数据断言，不能把 16 全算作记录间关系。

早期 `runs/mock-001` 因可编辑包安装尚未完成而未能启动 MCP，保留其 partial 记录；安装完成后的 `mock-002` 及本次运行成功。失败记录未覆盖或删除。

## 3. 规模与预算

| 导入总记录 | 场景 | 检查源记录 | 未处理源记录 | 模拟 LLM 调用 | 状态 | 耗时 | 主进程峰值内存 |
|---:|---|---:|---:|---:|---|---:|---:|
| 10,005 | 编码引用 | 10,000 | 0 | 6 | complete | 8.688 秒 | 201.03 MiB |
| 100,005 | 编码引用 | 10,000 | 90,000 | 6 | partial | 5.172 秒 | 214.83 MiB |
| 1,000,005 | 编码引用 | 10,000 | 990,000 | 6 | partial | 5.922 秒 | 365.39 MiB |
| 1,000,005 | 不同描述的文本查询 | 1,000 | 999,000 | 30 | partial | 1.168 秒 | 290.98 MiB |

每组包含 5 条目标定义。10,000 条场景全量物化输入对象；较大场景关闭全量物化，只输出实际关联涉及的对象，并按指定上限检查源记录。因此这些时长不能直接比较成吞吐率。耗时不含夹具生成，内存为主 Python 进程峰值，不含 MCP 子进程；这是本机单次原型记录，不是性能承诺。

百万条编码场景明确报告 **990,000 条未处理**；文本场景总模型调用停在 **30 次**，未处理 **999,000 条**，同时记录语义预算不足。这两项证明导入和预算限制生效，**没有完成百万条全量语义抽取**。

文本场景按记录生成不同描述，目标仍是 5 个合成条目；它用于验证疑难查询预算，不能代表百万个独立业务定义。此前百万条仅导入/统计的结果单独保留于 `runs/benchmark-1000000-import`，不与完整流程混用。

规模脚本和可复查的原始结果：

- [scripts/benchmark.py](../../route2/scripts/benchmark.py)
- [一万条完整流程](../../route2/runs/scale-10000-full/benchmark.yaml)
- [十万条受限执行](../../route2/runs/scale-100000-capped/benchmark.yaml)
- [百万条受限执行](../../route2/runs/scale-million-capped/benchmark.yaml)
- [百万条文本预算检查](../../route2/runs/scale-million-text/benchmark.yaml)

## 4. 资源检查

四个运行目录拥有独立源码和模型副本；路线1子路线1只含内部模型，其他三个目录均含四个参考模型。SAND、GRAMS、steiner-tree 仅位于路线3。

Git 提交、远程地址、工作区、对象完整性，以及所有模型文件的 SHA-256 均已核对。三份 RDF 主文件可解析；CDM 共 57,898 个 JSON 可解析，其中 2 个原文件需要 cp1252 解码，原文件未改动。具体来源和校验见 [resource_verification.yaml](../../resource_verification.yaml) 与各目录的 `resources.yaml`。

## 5. 尚未验证与实现边界

真实 LLM 需要用户配置 `.env`；真实企业 MCP 与真实业务数据尚未提供。后续应分别测候选召回、关系/条件正确性、根类型混淆和多次在线运行差异。当前没有以 Judge 通过代替这些指标。

复杂否定条件、任意公式语法、embedding、稀有模式自动分组及跨快照身份不属于当前已验证能力，状态见 [实现清单](implementation.md)。路线1、路线3已完成设计与资源准备，其改造引擎不在本次路线2代码实现范围内。
