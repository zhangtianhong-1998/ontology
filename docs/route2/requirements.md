# 路线2：需求与用户故事

本文中的代码、配置路径及命令均以仓库的 `route2/` 目录为基准。

本文中的对象、字段、公式和地区等均为假设性例子，不说明真实输入包含这些内容。运行时先检测证据和适用性；没有相应内容就不启用该提取器。目标模型的五类根也不要求输入覆盖全部类别。

## 1. 需要解决的问题

元数据一般足以说明表、列的用途。新增分析聚焦三个产物：

1. 定义对象：一条记录描述什么，如何保留其来源身份和类型依据。
2. 记录关系：具体记录引用或依赖哪些对象，引用来自哪个字段或定义。
3. 关系条件：哪些记录适用，范围来自哪些真实字段或显式规则。

没有显式外键和血缘时，检测输入实际存在的字段引用、文本、公式或上下文；仅使用其中可获得的证据发现候选。只使用实际存在的来源，不要求用户先补齐查询日志或 ETL 系统。

## 2. 用户故事及验收

| ID | 用户故事 | 验收条件 |
|---|---|---|
| R2-US01 | 作为开发者，我希望把已有元数据组织成可查询的图 | 生成 table/column/constraint/source 节点及声明关系；FK 空仍保留全部节点；不产生虚构 lineage |
| R2-US02 | 作为实验者，我希望记录之间的显式引用优先由程序发现 | 标识按类型、命名空间、版本和引用用途匹配；C03/C04 满足；目标多义时未决 |
| R2-US03 | 作为指标使用者，我希望知道某指标依赖哪几个度量 | 公式解析、符号绑定、来源定位完整；常数和函数名不会成为度量；C05 满足 |
| R2-US04 | 作为 API 使用者，我希望区分维度关联与取值限制 | C06/C07 同时满足；部分记录命中时只生成相应关系，不推广为全表外键 |
| R2-US05 | 作为实验者，我希望无公共编码的关系仍可被发现 | 名称、定义、单位、口径和上下文共同召回/判别；支持多候选和未决；C08/C09 满足 |
| R2-US06 | 作为开发者，我希望 LLM 只处理需要语义判断的部分 | 全量导入、匹配、解析由程序执行；相同模式复用计划；百万记录的 LLM 调用仍受硬上限约束 |
| R2-US07 | 作为模型使用者，我希望企业检索只补充相关证据 | 围绕缺失问题检索；最多五轮；摘要含原文定位、范围和反证；无有效资料返回空上下文；C10/C11 满足 |
| R2-US08 | 作为模型使用者，我希望外部本体使用内部业务术语 | 内部对象保持原 ID/术语；对齐保存 URI、范围、类型与依据；无依据不强行 exact |
| R2-US09 | 作为实验者，我希望每条关系都能解释成立条件 | 主体/关系/客体/作用域/条件/证据齐全；C12 满足；观察值不会升级为业务限制 |
| R2-US10 | 作为开发者，我希望错误可定位且结果可重放 | 每阶段保存统计、错误、未决原因和缓存键；超预算输出 partial；C15/C16 满足 |
| R2-US11 | 作为使用者，我希望没有示例业务模式的数据也能运行 | 无公式、无维度或无记录关系的合成输入正常结束；不生成示例类型或关系 |
| R2-US12 | 作为实验者，我希望填写 env 后直接切换真实 LLM | mock 与 AgentScope 共用数据契约；缺配置明确失败；本地兼容 HTTP 服务验证实际 SDK 请求与 token 统计 |
| R2-US13 | 作为开发者，我希望模拟 MCP 能验证真实协议 | 使用独立 stdio 服务进程；验证 search/fetch 和三轮检索；无关原文仅进审计，不进入本体生成上下文 |
| R2-US14 | 作为实验者，我希望统计范围和误差可辨别 | 区分 CSV 来源范围与实际扫描范围；NULL/空值约定、近似/精确、统计未完成状态明确；近似基数不证明唯一 |
| R2-US15 | 作为实验者，我希望少量特殊引用能进入候选 | 固定哈希、高频、稀有结构、反例样本；2% 引用案例不被整列低包含率排除；遗漏组数量可查 |
| R2-US16 | 作为开发者，我希望程序先产生字段关联候选 | 元数据、原值、结构及文本通道取并集；角色 unknown 有限回退；LLM 无需逐行或枚举全部字段对 |
| R2-US17 | 作为实验者，我希望关联统计不被重复值误导 | 行命中、不同键包含、唯一定位、多义分别报告；复合键和条件完整；前导零/拼接碰撞/空分母可检验 |
| R2-US18 | 作为开发者，我希望候选能安全转换为执行计划 | 字段真实存在、转换有版本、引用用途/条件有依据、候选精确检查完成；未检查候选不能作为 accepted 关系 |
| R2-US29 | 作为实验者，我希望专门的关联发现 Agent 提出可复用规则 | Agent 只读元数据、统计和记录；输出受限 YAML，程序全输入复验，保存规则版本、作用域和反例；无证据可返回未决 |
| R2-US30 | 作为建模者，我希望相关定义记录能跨表进入同一增量任务 | 定义记录与关联记录分开组包；已验证连接、名称/别名和语义相似的来源通道清楚；输出可追溯的业务概念与记录对齐，相似不直接认定同一概念 |
| R2-US31 | 作为开发者，我希望百万行中只编码和检索有语义价值的内容 | 全输入筛查后生成带出处的定义卡与轻量引用卡，按内容和范围去重；词法索引覆盖与向量编码范围分别报告，未入索引记录可定位 |
| R2-US32 | 作为实验者，我希望比较 BM25、向量及融合的实际增量 | 在相同输入与模型预算下比较结构/词法/向量/融合的 Recall@K、错误候选、耗时与磁盘；中文短词另测 |
| R2-US33 | 作为建模者，我希望一个包既有代表例也有反例 | 每包保留条件分支、目标定义、唯一匹配、歧义/缺失和口径冲突；选择原因、未选范围与完整请求字节数可查 |
| R2-US34 | 作为开发者，我希望组包输出能复用而不过期 | 包 ID 绑定快照、规则和记录集合；模型响应缓存另绑定 core、提示词、模型；本路线只在同快照内复用，新快照全部失效并重验规则 |
| R2-US35 | 作为实验者，我希望组级增量不会破坏已接受本体 | 多表任务只修改声明的表和类型；程序校验证据、条件与冲突，Judge 后原子合并；失败保留旧 core 和未决原因 |
| R2-US36 | 作为使用者，我希望预算用完后知道还缺什么 | 分开报告扫描记录、索引卡、候选、已验证规则、已送模型语义组及未处理组；不把 CSV 已扫描当作语义完成 |

新增 US19—US24 见 [RIGOR 对照与本轮改造](RIGOR_COMPARISON.md)：覆盖上游差异核对、确定性映射、逐表增量与回滚、原生 ReAct/MCP、知识过滤及结果可视化。实现及测试入口见 `incremental.py`、`knowledge.py`、`visualization.py` 和 `tests/test_incremental.py`。

## 3. 功能边界

- 元数据图不要求部署 OpenMetadata/DataHub；当前导出文件足够建立最小图。未来的 catalog 连接器只需转换到 SourceRef 和元数据节点，不改核心算法。
- 不做全字段通用数值画像。只计算关联需要的非空比例、不同值数、候选目标唯一性、包含率、标识格式和选定枚举等；全量统计与样本统计标明范围。
- 不把整张 CSV 发给 LLM。不为每一行启动 Agent。企业 MCP Agent 仍按有限工作单元运行；拟新增的关联发现 Agent 按候选规则工作，不按记录运行。
- 当前代码已在计划生成前提出有限字段候选并精确核验选中字段对，再交给 LLM 作语义判断；条件/复合键自动发现、有限补样和计划自动编译仍待实现。任意稀有模式的完整发现仍不保证，未覆盖组和未处理候选必须报告；导入记录数不等于关系召回率。
- 外部本体与企业文档分两条来源通道。外部模型提供建模参照，企业文档可能提供业务定义；后者也必须检查适用范围，不能天然覆盖源数据。
- 输出包含 ontology、业务定义对象、记录关系、条件、证据和未决项。路线2拟固化并复验本次快照内的关联规则；路线3再研究跨快照身份融合和更广的变更传播。

## 4. 非功能与接受要求

原型先验证小样本完整流程，再做 10 万、100 万记录的导入和受预算约束的执行。百万全量语义抽取单列为规模实验，不能由截断运行代替。目标定义与标识建索引，不枚举所有记录对。执行器每次读取 1,000 行，输出默认每片 5,000 项；预算以实际运行配置为准。

接口输出必须稳定可解析；语义结果允许未决。任何“低温度、重复投票、Judge 同意”均不能作为稳定正确的保证。评测应同时报告正确率、召回、未决比例和处理范围。

缺失文档不阻塞有证据的本地关系；MCP 故障与无结果分开。定义不足时不增加 LLM 重试来强迫其给出答案。

US14—US18 的实现程度不一：统计口径、基础候选和单列核验已有代码；频次分层、结构/文本候选和自动计划编译尚未完成。算法、数据契约和初始预算见[字段统计与关联发现设计](FIELD_PROFILING_DESIGN.md)，具体状态见[实现清单](implementation.md)。

US29—US36 是下一阶段需求。完整的实例检索、组包契约、成本边界和 IB0—IB4 对照见[实例组包设计](INSTANCE_BUNDLE_DESIGN.md)。当前代码尚未在 CSV 实例上运行 BM25+向量融合，也没有组级增量调度；不能用现有合成测试宣称上述需求已验收。

## 接口与命名需求

- US25：可通过配置选择流式和非流式接口；抽取和 ReAct 均使用相同设置，流式分片不提前成为本体事实。
- US26：可显式关闭或开启思考，也可保留服务端默认设置；提供 env.example，记录有效配置，不自动忽略被服务拒绝的参数。
- US27：三个路线的自研目录、文档和配置文件统一英文命名，原有引用和入口同步迁移。
- US28：路线2可在 Windows 11 x64、Python 3.14.7 上不依赖 Docker 运行模拟数据；提供目标平台离线轮子与可核验的安装包清单。元数据图仍由本地代码处理，可选导出 DataHub metadata-file 并在隔离环境的 Lite 中读回；不能把 Lite 当作完整图遍历/血缘服务。Windows 真机运行前仅称跨平台依赖解析与本地模拟验证已完成。

验收方法及参数见 [LLM_CONFIGURATION.md](LLM_CONFIGURATION.md)。字段发现升级项的未完成范围见 [STATUS.md](STATUS.md)。
