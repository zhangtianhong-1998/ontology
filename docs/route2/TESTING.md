# 代码包测试说明

本文中的代码、配置路径及命令均以仓库的 `route2/` 目录为基准；文档链接可直接点击。

在路线2目录执行 `uv sync --locked --extra test`、`uv run pytest -q`。

测试使用代码动态生成的中性合成数据、模拟响应、真实 stdio MCP 协议，以及本地 HTTP 模型服务。不会读取企业数据，也不调用外部 LLM。

- 增量构建：直接映射覆盖、当前 core 传递、Judge 修正、校验失败回滚、声明依赖顺序。
- 检索：AgentScope 原生 ReAct 工具调用、二次查询、原文读取、冲突摘要、引用校验及预算。
- 可选向量：开关与本地路径校验、精确余弦稳定排序、FTS 漏召回时外部本体向量补召回、core 仅从已接受表取语义邻居、MCP 已返回候选重排且保留原始名次。单测使用假向量，不需要下载模型。
- 记录处理：条件引用、歧义、公式/JSON 局部引用、未处理范围、跨 schema 表名和复合键。
- 可视化：预览数量限制和文本安全渲染。

`test_local_external_rdf_import` 使用已打包的 gist，可以在无网络环境直接执行。KPIOnto 正文未发现明确的再分发许可，需从上游直接取得；它不是基础测试的依赖，见[公开本体说明](../PUBLIC_ONTOLOGIES.md)。

版本 0.3.0 于 2026-09-23 在独立目录运行：**60 项通过、1 项明确跳过、0 项失败**，耗时 24.35 秒。跳过项仅为缺少文件的 KPIOnto 集成测试。22 项新增接口用例覆盖 JSON/SSE、思考控制、ReAct、断流与超时。完整本地工程含 KPIOnto，61 项全部通过。测试均使用本地模拟服务，未调用外部模型。自研代码与同步后的原型工程一致，历史运行结果不随包上传。

本次公开模型同步后，在 `xuanyue` 工作树重新运行：**75 项通过、0 项跳过、0 项失败**。其中外部 RDF 集成测试使用随仓库提供的 gist。上面的 0.3.0 记录是当时的历史结果，保留作对照。

另用 `runtime.mock.yaml --profile E2O` 做离线端到端检查，运行状态为 `complete`；本地读取 1 个 gist 文件、生成 212 张外部术语卡，导入错误为 0。这里验证的是读取与流程衔接，不代表术语对齐或本体内容已通过业务验收。

新增 `tests/test_embedding.py` 的 7 项单测已单独运行通过。本机现有 Qwen3-Embedding-0.6B 还通过一次离线加载 smoke：输出维度为 1024，“营业收入的定义”对相关收入文本和无关天气文本的余弦分别为 0.7564/0.1215。前者验证检索逻辑与边界，后者仅验证此目录的模型可本地加载和编码；两者都不是企业语料召回率、跨语言对齐质量或 Windows 3.14.7 兼容性证明。真实企业 MCP 只接受服务端返回的候选；客户端 embedding 可重排标题/片段，但不能证明服务端采用向量索引或找到未返回的文档。

本次向量接入后在 `xuanyue` 工作树运行路线2全套测试：**83 项通过、0 项跳过、0 项失败**。本地模型 smoke 使用已有 Python 环境中的 SentenceTransformers 5.7.0；`uv.lock` 新解析的可选依赖版本和 Windows 3.14.7 尚未完成实机集成验证。

这些测试验证代码行为和接口，不能证明真实模型的语义质量、企业文档有效性或百万条完整抽取性能。路线1和路线3目前为设计及依赖准备，不能按路线2测试通过推断其引擎已经实现。
