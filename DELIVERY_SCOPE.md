# 本次上传范围

来源：原工作区实现提交 `fdf82b9ac6260612dbd61827eb11fc40954b8d31`。本分支为独立的代码包历史。

| 纳入 | 排除 |
|---|---|
| 三个路线的需求、US、SDD、开发任务、算法设计 | 论文、翻译、研究产物、演示文稿 |
| 路线2自研代码、可视化模板、测试和数据生成代码 | CSV/YAML 输入数据、模拟数据文件、运行结果、日志、数据库 |
| 内部 YAML 类型契约、配置和环境变量示例、依赖锁 | 外部本体数据、`.env`、私有配置、虚拟环境和缓存 |
| 9 个固定版本的上游代码子模块、来源清单 | 原工作区 Git 历史及不属于三个路线的文件 |

路线1保留子路线1与子路线2；SAND、GRAMS、steiner-tree 仅在路线3配置。外部模型清单中的 `bundled: false` 表示文件需另外取得。

为了让代码包可以独立使用，README 和运行说明改为克隆后的相对路径，模拟数据改为通过 `make-demo` 生成；缺少 KPIOnto 时，其专用集成测试明确跳过。初次导出时自研引擎源码与来源提交一致；后续更新见 Git 提交记录。

仓库维持私有。原 `main` 分支仍保留之前的研究资料；默认分支 `codex/three-routes` 只展示本次代码包。
